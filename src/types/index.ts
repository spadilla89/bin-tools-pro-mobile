// Types and Interfaces Definitions

interface BinData {
  bin: string;
  bankName: string;
  cardBrand: string;
  cardType: string;
  countryCode: string;
}

interface GeneratedCard {
  //... properties for GeneratedCard
}

interface ScraperConfig {
  //... properties for ScraperConfig
}

interface LiveFinderConfig {
  //... properties for LiveFinderConfig
}

interface AddressData {
  //... properties for AddressData
}

interface StorageItem {
  //... properties for StorageItem
}

interface AppSettings {
  //... properties for AppSettings
}

type RootTabParamList = {
  //... navigation types for RootTabParamList
};